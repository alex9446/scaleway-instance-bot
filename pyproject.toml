[tool.poetry]
package-mode = false

[tool.poetry.dependencies]
python = "^3.12"
fastapi = "^0.141.1"
python-telegram-bot = "^22.8"
uvicorn = "^0.52.4"
scaleway-async = "^2.11.0"

[tool.poetry.group.dev.dependencies]
isort = "^8.0.1"
flake8 = "^7.3.0"
pyright = "^1.1.411"
requests = "^2.34.2"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
